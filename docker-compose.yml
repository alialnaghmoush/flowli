services:
  redis:
    image: redis:8-alpine
    container_name: flowli-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 2s
      retries: 15
    command: ["redis-server", "--appendonly", "yes"]
